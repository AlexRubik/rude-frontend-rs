{
  "name": "solana-web-app",
  "version": "0.1.0",
  "description": "A Solana-focused web app built with Rust and Dioxus",
  "scripts": {
    "build:css": "npx tailwindcss -i ./input.css -o ./public/tailwind.css --config tailwind.config.js --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
